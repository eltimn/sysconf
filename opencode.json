{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "nixos": {
      "command": ["nix", "run", "github:utensils/mcp-nixos", "--"],
      "enabled": true,
      "type": "local"
    }
  }
}
